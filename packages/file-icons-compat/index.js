'use strict'

// @marktext/file-icons ships its runtime JS, CSS and fonts prebuilt. Its
// file-icons dependency is only used to produce those checked-in assets, so
// LeafBook intentionally supplies an inert local package at runtime.
module.exports = Object.freeze({})
